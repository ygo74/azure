
stages:
  - stage: deploy_aks
    displayName: Deploy AKS Bootstrap

    jobs:
      - job: create_cluster
        displayName: Create Aks Bootstrap
        pool:
          vmImage: 'ubuntu-latest'

        steps:
          - script: echo hello from Linux

          - task: ms-vscs-rm.vss-services-ansible.ansible-task.Ansible@0
            displayName: 'Create cluster'
            inputs:
              playbookPathOnAgentMachine: aks_create_cluster.yml
              inventoriesAgentMachine: file
              inventoryFileOnAgentMachine: inventory
              args: '-e test=xxx'
              workingDirectory: ansible